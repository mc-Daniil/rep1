services:
  stack2:
    build: .
    ports:
      - "1341:1341"
    environment:
      - FLAG=flag{redacted}
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true